/*
#pragma once
#include <string>
#include <SOIL.h>
#include <GL/glew.h>
#include <iostream>
#include "Texture.h"

class TextureObj : public Texture
{
public:
    TextureObj(const string& path);
    void bind(GLenum textureUnit = GL_TEXTURE0) const override;
    void unbind() const override;

};
*/
